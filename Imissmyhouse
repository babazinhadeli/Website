<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>I Miss My Cafe - Soundboard</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <h1>I Miss My Cafe</h1>
    
    <div id="soundboard">
        <div class="sound-item">
            <audio id="sound1" src="your-audio-url.mp3"></audio>
            <button onclick="togglePlayPause('sound1', 'button1')">Play Barista</button>
            <input type="range" min="0" max="1" step="0.01" value="1" id="volume1" onchange="setVolume('sound1', 'volume1')">
        </div>

        <div class="sound-item">
            <audio id="sound2" src="your-audio-url.mp3"></audio>
            <button onclick="togglePlayPause('sound2', 'button2')">Play Coffee Cups</button>
            <input type="range" min="0" max="1" step="0.01" value="1" id="volume2" onchange="setVolume('sound2', 'volume2')">
        </div>
        
        <!-- Add more sound elements as needed -->
        
        <button onclick="pauseAll()">Pause All</button>
    </div>

    <script src="script.js"></script>
</body>
</html>
body {
    font-family: Arial, sans-serif;
    text-align: center;
    background-color: #f8f3e6;
    color: #333;
}

h1 {
    font-size: 3rem;
    margin-top: 20px;
}

#soundboard {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    margin-top: 50px;
}

.sound-item {
    display: flex;
    align-items: center;
    gap: 15px;
}

button {
    padding: 10px 20px;
    background-color: #ffcc00;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
}

button:hover {
    background-color: #ff9900;
}

input[type="range"] {
    width: 200px;
}
function togglePlayPause(audioId, buttonId) {
    var audioElement = document.getElementById(audioId);
    var buttonElement = document.getElementById(buttonId);

    if (audioElement.paused) {
        audioElement.play();
        buttonElement.textContent = "Pause";
    } else {
        audioElement.pause();
        buttonElement.textContent = "Play";
    }
}

function setVolume(audioId, volumeId) {
    var audioElement = document.getElementById(audioId);
    var volume = document.getElementById(volumeId).value;
    audioElement.volume = volume;
}

function pauseAll() {
    var audios = document.querySelectorAll("audio");
    audios.forEach(audio => {
        audio.pause();
    });

    var buttons = document.querySelectorAll("button");
    buttons.forEach(button => {
        if (button.textContent === "Pause") {
            button.textContent = "Play";
        }
    });
}
